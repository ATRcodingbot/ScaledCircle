'use strict';
const {test}=require('node:test'),assert=require('node:assert/strict');
const p=require('./live_work_certification_policy'),gate=require('./paid_work_launch_gate');
const now=1789308000000,c={version:p.VERSION,enabled:true,project:'scaled-circle',appEnv:'production',permitId:'single_permit_000001',businessUid:'business_owner_000001',scalerUid:'scaler_worker_000001',campaignId:'private_campaign_000001',zoneId:'private_zone_000001',baseCents:300,bonusCents:0,maximumChargeCents:360,issuedAt:now,expiresAt:now+86400000};
const campaign={businessId:c.businessUid,intendedScalerId:c.scalerUid,liveWorkCertificationDigest:p.binding(c),liveWorkCertificationId:c.permitId,basePay:3,bonus:0,workerAmountCents:300,fundingVersion:0,campaignType:'yardCleanup',certificationFixture:true,marketplaceVisible:false,trackingEnabled:false};
const input={raw:c,now,campaignId:c.campaignId,campaign,actorUid:c.businessUid};
test('single exact Founder job can be authorized independently while global production paid-work hold stays closed',()=>{assert.equal(p.funding(input).campaignId,c.campaignId);assert.throws(()=>gate.assertNewPaidWork({project:'scaled-circle',enabled:'false'}));});
test('wrong owner, Scaler, campaign, quote, reuse version, environment, digest, expiry and public exposure all fail closed',()=>{for(const patch of [{actorUid:'other'},{campaignId:'other'},{now:c.expiresAt},{raw:{...c,project:'scaledcircle-staging'}},{raw:{...c,enabled:false}},{raw:{...c,expiresAt:now+15*86400000}}])assert.throws(()=>p.funding({...input,...patch}));for(const patch of [{intendedScalerId:'other'},{businessId:'other'},{basePay:4},{bonus:1},{workerAmountCents:301},{fundingVersion:2},{liveWorkCertificationDigest:'changed'},{marketplaceVisible:true},{trackingEnabled:true},{campaignType:'neighborhoodCanvassing'}])assert.throws(()=>p.funding({...input,campaign:{...campaign,...patch}}));});
