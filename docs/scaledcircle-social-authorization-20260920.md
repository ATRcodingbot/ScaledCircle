# ScaledCircle automatic strategy authorization — diagnosis / pending boundary choice

September 20, 2026. This is NOT an authorization-ready or deployed repair report.

Production Social owner is `p1tigN2XE9ascVN4es3RBncw6nf1`, with maintained Admin role and active user record. The exact UID and ScaledCircle Facebook/Instagram identities are bound in `socialProviderConfigs/production_meta.metaDogfood`. No email-string permission inference is appropriate. This is separate from Attractive Remodel and the staging Growth workspace `FF1bfDuvtdNjuuC4mc7NdGtk3LC3`.

Cause: `metaCustomer.available` excludes Admin roles, hiding the automatic-publishing card. The callable additionally requires role business, and its settings proposal requires an active customer Managed Growth entitlement, `businessGrowthProfiles`, and an approved plan with `strategy.services`. The production internal Social owner has none of these customer records. Simply exposing the button would still fail; inventing a customer subscription or borrowing staging/Attractive Remodel context is prohibited.

Three cards are distinct legacy channel plans, not duplicates: X `social_plan_41c51bb1efde3222c8aa7281e5e135ea4640e940`; Instagram `social_plan_485fe0e5258341340d110e81738286f7e5500c56`; Facebook `social_plan_f4fadb8deab4750875b0ac5e26943ca99c48730a`. Each is version 1, starts September 7, approved for its old manual workflow. The accompanying cycles are draft/approval_required and have America/New_York timezone. There is no new socialManagedPolicies record. These are not proof of bounded autonomous strategy authorization.

Meta context: product/authority, Business value, and the two-sided Business/Scaler model; Maryland audience; maintained ScaledCircle destinations. Read-only provider records show Facebook Scaled Circle and Instagram @scaledcircleapp connected_write/healthy. X remains distinct and must not be automatically included. Existing config is identity authority, not itself owner strategy approval.

Pending question asks Founder whether to use those existing Facebook/Instagram topics and destinations for the new confirmation or supply updated strategy boundaries. Do not manufacture a current brand profile/managed strategy silently. Final confirmation remains Founder-only. No policy, entitlement, profile, plan, job, provider credential or financial record changed during this diagnosis.

Prepared presentation changes (not deployed): channel/date/version/current-plan labels distinguish the three cards; page shows canonical workspace name; server name fallback uses the resolved owner's maintained companyName. One focused plan-identity test passes, analyzer clean. No authorization-ready claim. Shared Flutter changes belong in the next matched native pair, not a standalone rebuild.

Next implementation must connect the exact maintained internal owner/config authority to the existing authorization system, with reviewed strategy compatibility and end-to-end worker entitlement checks; it must not merely remove the UI gate or the role check. Preserve ordinary customer eligibility and all existing policies. Tests still needed: internal owner/no policy, wrong Admin/member/workspace, post-confirmation audit/state, recurring runtime access and unchanged Attractive Remodel. No deployment performed for this incomplete path.
