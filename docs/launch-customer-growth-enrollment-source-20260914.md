# Customer Growth enrollment source correction — September 14

Source only; not a production enrollment or availability certification.

The accepted b120 preparation removes purchase-time private access for Managed Growth and the Lead Generation add-on. It does not graduate runtime cohorts.

This bounded correction replaces Customer Growth's invitation check with maintained server workspace and subscription authority. Active Managed Growth can open its Growth coordination workspace. Active paid Lead Generation purchasers can open it on Starter, Growth, or Scale without a manual invitation. Research and prospect review require `hasActiveProductEntitlement(..., 'lead_generation_research')`, including current active paid membership, expiry, Stripe source, and both reconciled add-on and product entitlement fields. Managed Growth alone cannot run paid Lead research. Growth Department continues to use the existing billing projection into Managed Growth plus its included add-ons; no billing mapping or prices changed.

Customer research report queueing now rechecks the current paid Lead entitlement in its Firestore transaction, instead of requiring a customer invitation. Owner identity, verified account, report preferences, tenant isolation, deduplication, and all existing send/approval boundaries remain in place. No mailbox connection authorization changed; normal new-customer Gmail onboarding remains held.

Validation: 8 focused Customer Growth emulator tests passed with no skips, including normal lower-plan enrollment, denial after cancellation/expiry or missing paid entitlement, Managed Growth-only research denial, owner/team authority, legal consent, source/geography/pause limits, idempotency, and report suppression after entitlement removal. 8 Customer Growth/subscription unit tests passed; generated codebase verification passed.

## Still required before production graduation

- Deploy and read back the reviewed `customerGrowthOperationsV1` and `queueCustomerGrowthReportEmailV1` source delta. This task performed no deployment or real subscription mutation.
- Social source now uses current Managed Growth entitlement for OAuth, planning, editing, image preparation and delivery. Scheduling can use the same authority when explicitly activated with `SOCIAL_CUSTOMER_ENROLLMENT_MODE=plan_entitled`. With that mode unset or unknown, the scheduling store and publisher retain the existing `SOCIAL_CUSTOMER_SCHEDULING_UIDS` cohort. Deploying source alone does not expand publisher execution.
- In plan-entitled mode, `runCustomerMetaPublisherV1` selects at most 100 current-window due owner-approved customer jobs and separately up to 25 expired approvals for review. Four bounded workers process exact selected job IDs; empty subscribers and historical posts cannot delay selected work. Expired approvals move transactionally to the existing `authority_review_required` state and existing grouped attention notifications, with zero provider calls. Successful jobs leave the approved query, so no persisted subscriber cursor exists. The existing runtime still rechecks current membership and exact owner approval, provider permission/configuration and kill switches before provider work. Inventory selection itself creates no approvals or jobs. Internal dogfood and X remain separate.
- Creative generation supports `plan_entitled` but actual production rollout mode and budget/cap configuration still need maintained readback and graduation.
- Provider OAuth/new-sender onboarding remains independent of product purchase. Do not imply the Google approval process has completed.

## Coordinated Social verification and activation

Focused Social emulator coverage passed: editor and media transactions for an unlisted eligible Business, denial for canceled/lower-plan subscriptions, exact scheduling/idempotency, isolation, due-work selection among 125 empty subscribers, more than 25 due tenants, and expired-window review, and OAuth recovery/connection/planning without publication. The initial combined run passed 27 of 28 tests; the remaining test exposed a missing Firebase Auth mock in the OAuth harness after normal enrollment reached the store. After adding the mock, all 10 OAuth tests passed. No skipped tests. The other 18 tests had already passed. Another 32 Social eligibility, generation and scheduler unit tests passed, plus 3 tests exercising the actual scheduler handler and exact document selection with unset/unknown versus explicitly enabled enrollment mode. Generated verification and syntax/diff checks passed.

Activate `SOCIAL_CUSTOMER_ENROLLMENT_MODE=plan_entitled` consistently on `getSocialOperationsWorkspace`, `previewCustomerSocialPostV1`, `approveAndScheduleCustomerSocialPostV1`, `prepareCustomerSocialPostV1`, and `runCustomerMetaPublisherV1` only after reviewed rollout authorization. Existing `agentHealth` safety holds and `socialProviderConfigs/production_meta` settings remain unchanged. No new globally enabled flag is written by source.

Generation's existing maintained `providerConfigurations/generated-service-visuals` authority must separately graduate from `beta_cohort` to `plan_entitled`. Preserve existing `providerGenerationEnabled`, provider/global safety caps and exact 60-source-concept Managed Growth allowance; lower plans still receive zero included units. Parent production readback found daily cost cap 10,000,000 micros and monthly cap 100,000,000 micros, both unchanged by this task.

Reviewed normal customer deployment export list: `customerGrowthOperationsV1`, `queueCustomerGrowthReportEmailV1`, `previewCustomerSocialPostV1`, `approveAndScheduleCustomerSocialPostV1`, `prepareCustomerSocialPostV1`, `serveCustomerSocialMediaV1`, `getSocialOperationsWorkspace`, `prepareCustomerSocialPlanV1`, `approveSocialContentPlanV1`, `beginSocialOAuthConnectionV1`, `getSocialOAuthAttemptV1`, `cancelSocialOAuthAttemptV1`, `confirmSocialOAuthConnectionV1`, `syncMetaSocialReadOnlyPerformanceV1`, and `runCustomerMetaPublisherV1`. No deployment occurred here.

The due-work query requires the added Firestore composite index on `socialGrowthJobs` (`customerApproval ASC`, `status ASC`, `scheduledFor ASC`) to be ready before activating plan-entitled scheduling. Deploy only this reviewed index delta through the maintained index configuration after comparing live indexes. Without it the newly activated query fails closed; default cohort scheduling does not use it. The additional due-inventory/missed-window emulator test passed after this correction, as did the selected-job scheduler unit tests. No production index or setting was changed.

Final combined verification after all source corrections: 36 changed-backend emulator tests and 43 selected unit tests passed, zero skipped; generated verification passed. Parent subsequently requested the single reviewed production index after read-only comparison; readiness is a rollout prerequisite. No Function or cohort/generation setting was changed by this source checkpoint. A known nonblocking cleanup limitation remains: expired jobs for revoked subscriptions can occupy the expired cleanup page, but do not block the separate current due-work query or authorize publication.

## Exact live-selector reconciliation

The production inventory contains 248 Functions across all regions. Five proposed exports are absent and are excluded from this existing-runtime release: reviewScheduledSocialContentV1 and rateHistoricalSocialContentV1 are legacy/internal gated paths; proposeScheduledSocialReplacementV1 has no mobile caller; syncSocialReadOnlyPerformanceV1 is the held YouTube path; createSocialContentPlanV1 is an unavailable generic plan creator. The customer empty-plan button must use the existing prepareCustomerSocialPlanV1 authority before release. No absent export will be provisioned to bypass this discrepancy. The package guard stopped before deployment when the original 20-selector list matched only 15.

The reviewed production composite index completed READY: socialGrowthJobs/CICAgNjaxJEK. No scheduler activation follows merely from index readiness.
