# Launch repair checkpoint — September 19, 2026

Starting branch: `real-completion-proof`, local/origin source `687db21518a535af8c179b69560da2df9e227b3d`. The starting tree was clean: the interrupted September 14 work was already committed. Current follow-up changes are being preserved and reviewed together. This is not a final deployment or launch certification.

## Current Google readback

Production project `scaled-circle`, authenticated Google Console, September 19:

- Verification Center: branding verified and shown to users.
- Data access not verified; review required for sensitive/restricted scopes.
- Data Access lists exactly `openid`, canonical `userinfo.email`, `gmail.send` (sensitive), and `gmail.readonly` (restricted).
- Send/read justifications remain saved. Restricted use is Email productivity.
- Demo YouTube URL is empty. No review submission was claimed or performed.
- No OAuth client, callback, scope, token, or mailbox connection changed during this readback.

The real grant-flow/functionality video and Google's required review remain external gates. Existing preparation: `google-business-email-demo-20260914.md`. New-customer Gmail onboarding must remain gated.

## Proven repair causes

- Campaign map: campaign-only queries could not prove the Rules' workspace constraint. All four queries now include canonical workspace and campaign IDs. Production readback also found a deployed account-closure guard missing from source; preserve it before deploying Rules.
- Membership: canonical membership route and legacy hash form were not registered consistently. Production Attractive Remodel owner reproduced the unavailable screen before repair.
- Property comparison: required the user to analyze a second polygon. It now asks the server to find bounded nearby alternatives inside saved geography. Recommendations have a 90-day reuse cooldown with immutable observations; active campaign overlap remains excluded.
- Customer Social scheduler: queried `approved`, while maintained approval writes `scheduled`. Inventory now includes both; expired jobs require review before provider access.
- ScaledCircle media: five immutable image URLs returned SPA HTML. Exact original files with matching filename SHA-256s are now included in Flutter web assets so later Hosting releases preserve them.
- Internal Growth: aliases incorrectly selected customer mode for authenticated internal Admin; existing authoritative internal bridge remains the authority.

## Independent runtime evidence

Read-only evidence: `.firebase/repair-agent-runtime-20260919/`.

Internal research completed September 18 at 13:00:22 UTC with no active lease. Social scheduler HTTP 200 runs continued every five minutes on September 19. Internal Facebook approved queue is exhausted; two expired Instagram jobs require review and must not be retried automatically. Customer recurring research was not wired, and Attractive Remodel lacked the separate Lead entitlement. Founder explicitly authorized an Attractive Remodel-only audited internal Lead grant; normal Managed Growth does not include the $699 add-on.

## Deployment baseline

Fresh September 19 readback: 248 production Functions; Hosting `sites/scaled-circle/versions/4f3be440ee0fd6ee`; production Rules `d96f9153-f622-45e9-9c1e-9a5dcfba5a33`; staging Rules `151c7981-b586-4e80-b8aa-c4b539ae834f`. Captured in `.firebase/launch-repair-20260919/`. No current repair deployment yet.

Paid Scaler work remains held. No new campaign, send, approval, publication retry, Stripe object, earnings, or Wallet operation has been performed by this repair.
